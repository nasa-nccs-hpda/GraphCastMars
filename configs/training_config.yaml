# Training configuration
checkpoint_path: /discover/nobackup/jli30/systest/Graphcast_Mars_test/checkpoints/graphcast/params_GraphCast_small.npz
stats_dir: /discover/nobackup/jli30/systest/Graphcast_Mars_test/checkpoints/graphcast
data_dir: /discover/nobackup/jli30/systest/Graphcast_Mars_test/checkpoints/data
output_dir: ./output/training

# Training hyperparameters
num_epochs: 100
batch_size: 1
learning_rate: 0.0001
weight_decay: 0.01
gradient_clip: 5.0

# Data split
#train_split: 0.8
target_lead_times: "6h" 

# Early stopping
#patience: 10

# Checkpointing
save_every: 10  # Save every N steps